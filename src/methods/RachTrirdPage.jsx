import React from 'react'
import { inject, observer } from 'mobx-react'
import * as R from 'ramda'

const RachTrirdPage = () => {
  return (
    <div>

    </div>
  )
}

export default RachTrirdPage|> observer |> inject('valuesStore')
